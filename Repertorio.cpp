//
// Created by yitzh on 11/07/2024.
//
/*
#include "Repertorio.h"

void usarRepertorio() {
    Repertorio* repertorio = Repertorio::getInstancia();
    repertorio->leerCSV("C:/Users/LENOVO/Desktop/Proyecto_progra_3/mpst_full_data.csv");

    // Ejemplo para verificar si las películas se han cargado correctamente
    const auto& peliculas = repertorio->getPeliculas();
    for (const auto& par : peliculas) {
        const Pelicula& pelicula = par.second;
        std::cout << "ID: " << pelicula.getId() << " Titulo: " << pelicula.getTitulo() << std::endl;
    }
}

*/
